# Copy this file to secrets.toml and fill in real values.
# secrets.toml is gitignored — never commit it.
# On Streamlit Community Cloud, set these via the Secrets UI in your app settings.

ANTHROPIC_API_KEY = "..."
APP_PASSWORD      = "..."
